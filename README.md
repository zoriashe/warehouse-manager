# Система расчета площади и вместимости штабелей для тар

## Описание

Приложение для автоматического расчета и оптимизации размещения тар на складских штабелях с учетом следующих правил:

- ✅ **Тяжелые тары всегда размещаются внизу** - автоматическая сортировка по весу
- ✅ **Тары с приоритетными деталями в доступе** - размещаются на средних уровнях для быстрого доступа
- ✅ **Верхняя полка зарезервирована для пустой тары** - оптимизация пространства

## Возможности

- 🧮 **Интерактивный калькулятор** - введите параметры вашего стеллажа и тар
- 📊 Расчет площади штабелей и полок с учетом габаритов (ДШВ)
- 📦 Автоматическая организация тар по правилам размещения
- 📈 Статистика использования площади и веса
- 🎯 Приоритизация тар с важными деталями
- ⚖️ Контроль максимальной нагрузки на полки
- 📋 Детальный отчет о размещении
- 💡 Рекомендации по оптимизации

## Установка и запуск

### Требования

- Python 3.7+

### Запуск приложения

```bash
python3 storage_calculator.py
```

При запуске выберите режим:

- **Режим 1** - Интерактивный калькулятор (введите свои данные)
- **Режим 2** - Демонстрация на примере

📖 **Подробная инструкция**: см. файл [USAGE.md](USAGE.md)

## Использование

### Режим 1: Интерактивный калькулятор 🧮

Введите параметры вашего стеллажа и тар:

```bash
python3 storage_calculator.py
# Выберите: 1
```

**Что нужно ввести:**

1. **Параметры стеллажа**: название, длина, ширина (в см)
2. **Параметры полок**: количество, для каждой - высота и макс. нагрузка
3. **Параметры тар**: для каждой - название, габариты (ДШВ в см), вес (кг), тип

Программа автоматически:

- Рассчитает коэффициент заполнения
- Разместит тары по правилам
- Покажет детальную статистику
- Даст рекомендации по оптимизации

📖 Подробная инструкция в файле [USAGE.md](USAGE.md)

### Режим 2: Демонстрационный пример

Готовый пример с 11 тарами различных типов:

```bash
python3 storage_calculator.py
# Выберите: 2
```

### Создание собственной конфигурации

```python
from storage_calculator import StorageStack, Container

# Создаем штабель (размеры в см)
stack = StorageStack(name="Мой штабель", base_length=200, base_width=120)

# Добавляем полки (снизу вверх)
stack.add_shelf(max_weight=500, height=50)  # Нижняя - для тяжелых
stack.add_shelf(max_weight=300, height=50)  # Средние уровни
stack.add_shelf(max_weight=300, height=50)
stack.add_shelf(max_weight=100, height=50, reserved_for_empty=True)  # Верхняя - для пустых

# Создаем тары
containers = [
    Container("T001", "Тяжелая тара", weight=80, length=60, width=40, height=45,
              content="Металлические детали"),
    Container("T002", "Срочная", weight=30, length=40, width=30, height=35,
              priority_parts=True, content="Срочный заказ"),
    Container("T003", "Пустая", weight=5, length=40, width=30, height=30,
              is_empty=True),
]

# Размещаем тары
placement_log = stack.organize_containers(containers)

# Выводим результаты
stack.print_layout()
stack.print_statistics()
```

## Структура классов

### Container (Тара)

Параметры:

- `id` - уникальный идентификатор
- `name` - название тары
- `weight` - вес в кг
- `length`, `width`, `height` - размеры в см
- `is_empty` - пустая тара или нет
- `priority_parts` - содержит ли приоритетные детали
- `content` - описание содержимого

### Shelf (Полка)

Параметры:

- `level` - уровень полки (0 - нижний)
- `max_weight` - максимальная нагрузка в кг
- `length`, `width`, `height` - размеры в см
- `reserved_for_empty` - зарезервирована для пустой тары

### StorageStack (Штабель)

Основной класс для управления размещением тар:

- `add_shelf()` - добавить полку
- `organize_containers()` - автоматическое размещение
- `print_layout()` - вывод раскладки
- `print_statistics()` - статистика использования
- `get_statistics()` - получить данные статистики

## Правила размещения

1. **Сортировка по весу**: Тяжелые тары размещаются на нижних полках для устойчивости конструкции

2. **Приоритетный доступ**: Тары с флагом `priority_parts=True` размещаются на средних уровнях для обеспечения быстрого доступа

3. **Резервация для пустых тар**: Верхняя полка автоматически резервируется для пустой тары

4. **Контроль нагрузки**: Автоматическая проверка максимального веса на каждой полке

5. **Оптимизация площади**: Расчет и контроль занимаемой площади на каждой полке

## Пример вывода

```
================================================================================
ПРОЦЕСС РАЗМЕЩЕНИЯ:
================================================================================
✓ Тара большая №1 размещен на полке 0 (обычная)
✓ Тара большая №2 размещен на полке 0 (обычная)
✓ Тара средняя №1 размещен на полке 1 (обычная)
✓ Тара срочная №1 размещен на полке 2 (ПРИОРИТЕТ - в доступе)
✓ Тара срочная №2 размещен на полке 2 (ПРИОРИТЕТ - в доступе)
✓ Тара пустая №1 размещен на полке 4 (пустая тара)
✓ Тара пустая №2 размещен на полке 4 (пустая тара)
✓ Тара пустая №3 размещен на полке 4 (пустая тара)

================================================================================
СТАТИСТИКА: Штабель А1
================================================================================
Всего полок: 5
Общая площадь: 12.00 м²
Занято: 5.64 м² (47.0%)
Свободно: 6.36 м²
Всего тар: 11
Общий вес: 438.0 кг
================================================================================
```

## Расширение функционала

Вы можете легко расширить функционал, добавив:

- Визуализацию штабелей
- Экспорт в Excel/CSV
- Базу данных тар
- Web-интерфейс
- Интеграцию со сканером штрих-кодов
- Систему уведомлений о заполнении

## Лицензия

MIT License - свободное использование

## Автор

Создано для оптимизации складского учета
# Updated Wed Dec  3 20:08:38 +05 2025
